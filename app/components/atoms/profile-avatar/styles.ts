import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  avatar: {
    alignSelf: "center"
  },
  editButton: {
    position: "absolute",
    alignSelf: "flex-end",
    bottom: 0
  }
});

export default styles;
