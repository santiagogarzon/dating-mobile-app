import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  avatar: {
    width: 40,
    height: 40,
    marginRight: 10
  },
  dateContainer: {
    flexDirection: "row",
    alignItems: "center"
  },
  dateText: {
    textAlign: "right",
    minWidth: 64
  }
});

export default styles;
